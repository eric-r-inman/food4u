#+title:     Changelog
#+language:  en

* Upcoming
** Added
1. In select mode, shift-clicking an item now toggles its whole category: every
   item in that category, pane, or recipe becomes selected, and when all of them
   already are, the same shift-click unselects them all. Selections elsewhere
   are left alone, and it works in every column. No action is needed.
** Fixes
** Breaking
** Maintenance
* v0.28.0
** Added
1. Two Shopping List departments are renamed: "Baking & Spices" becomes "Cooking
   & Baking", and "Condiments & Sauces" becomes "Condiments & Dressings".
   Cooking essentials — the oils, vinegars, ghee, and cooking wine — now sort
   into Cooking & Baking; canned tomato paste and chipotles sort with the canned
   goods; and fresh avocado and lemon sort with the produce. Existing kitchens
   pick up the renames automatically on their next server start, including their
   own category panes and sorting overrides. No action is needed.
* v0.27.0
** Added
1. The app grows hotkeys: "s", "c", and "p" toggle Select, Count, and Pare, and
   "1" through "5" open or close the columns, counted left to right in your
   current arrangement. Nothing fires while you are typing in a field, and each
   toggle's tooltip names its key. No action is needed.
* v0.26.0
** Added
1. The Shopping List sorts itself into grocery departments: every catalog food
   carries a store department, Auto-sort files the main bucket's items into
   their departments' panes, and each pane's Sort-here button re-targets the
   items now inside it, custom items included. Existing kitchens get the
   defaults automatically on their next server start.
* v0.25.0
** Added
1. The bulk-move button that appears while items are selected now shows how many
   it will move — "⬇ (3)" — instead of the "Move here" label, so the arrow reads
   at a glance as "move the selected items here". No action is required.
2. Plain olive oil joins the catalog's plant-based oils and fats beside the
   extra-virgin bottle, so a kitchen can track both. Existing kitchens keep
   their foods; the addition reaches only kitchens seeded afterward.

* v0.24.0
** Added
1. The bundled collection grows by thirty-seven recipes — eight more for each of
   MIND, DASH, Anti-Inflammatory, and Mediterranean, plus five built around tofu
   under a new Tofu tag — and the catalog gains red curry paste and curry powder
   to cook them from. Existing kitchens keep their recipes; the additions reach
   only kitchens seeded afterward.
* v0.23.0
** Added
1. Pasting a recipe now reads item counts: "3 red bell peppers" arrives as one
   chip counted three, and counted units like cans and cloves count too, while
   amounts like cups or grams stay a single item. The bundled recipes carry the
   same counts, so their ingredients land on the Shopping List in the quantities
   the dishes actually call for. No action is required.
* v0.22.0
** Added
1. The Staples Tracker's Auto button now stocks only shelf-stable proteins.
   Fresh cuts have given way to the canned fish a pantry actually keeps, so an
   auto-filled tracker no longer suggests meat you would buy for the week's
   cooking. No action is required.
2. Auto-stocking MIND now adds spinach, so the diet's most heavily weighted food
   group is represented for the first time. Drag it to the Freezer if that is
   how you keep it. No action is required.
3. The bundled default catalog absorbs a round of curation from daily use: a
   refreshed supplements shelf (powders, everyday vitamins, and extracts in; a
   few exotics out), pantry basics (salt, white flour, chicken broth, chili
   sauce, cranberry juice, canned herring), singular spellings for lemon,
   jalapeno, and radish, and the retirement of the generic onions entry and a
   duplicate parsley in favor of the specific forms already present. The bundled
   recipes and Auto staples follow the renames, so their chips keep their tier
   colour, stock tracking, and shopping-list identity. Existing kitchens keep
   their foods; the changes reach only kitchens seeded afterward.
* v0.21.0
** Maintenance
1. Git works inside the development shell on macOS again. The shell now supplies
   its own, because the Apple SDK it exports left the system one unusable there.
   No action is required.
* v0.20.0
** Maintenance
1. A checkout that loads its dev shell through direnv no longer reports a dirty
   working tree — the cache direnv builds from the checked-in .envrc is now
   ignored. No action is required.
2. The project is synced up to the project template's v0.72.0 release, which
   realigns the shared foundation and the macOS cross-build. No action is
   required.
3. Rust changes are now gated on clippy before review, using the same command CI
   enforces. Expect the gate to block a change until its warnings are cleared.
* v0.19.0
** Maintenance
1. The About page copy is rewritten to read tighter and clearer, and its
   redundant "philosophy" section — which only restated the foods-first idea
   already described above it — is dropped.
* v0.18.0
** Fixes
1. In select mode, the green control that moves the selection onto a category,
   pane, or Shopping List section now reads "⬇ Move here" instead of a bare
   down-arrow. Sitting a bare arrow next to a category's food count made it look
   as though every food in the category was selected; the label reads plainly as
   an action.
2. "Move here" now works between Longevity Foods categories. Selecting a food in
   one category and moving it to another did nothing, because the food already
   lived in the pyramid and the add guard rejected it as a duplicate. A move
   within the pyramid now relocates the food's own entry — out of its old
   category and into the chosen one — keeping its stock and recipe links.
** Maintenance
1. Re-sync the CI and Dependabot auto-merge workflows with the project template
   after it reworked its reusable auto-merge into a single-flight queue. The
   template's CI now carries a workflow_dispatch trigger, since the auto-merge
   flow re-dispatches CI against the changelog commit it writes, and the
   auto-merge wrapper gains issues: write and drops the job gate that moved into
   the reusable workflow. Because the wrapper pins the reusable workflow at the
   template's main, the drift had started failing the auto-merge run at startup
   on every CI completion — the "no jobs were run" notifications — and left
   Dependabot bumps unmerged.
* v0.17.0
** Added
1. The Shopping List's quick-add field now autocompletes. As you type, matching
   catalog foods drop down beneath the field — the ones that begin with what you
   typed first — and clicking one adds it straight to the list. Foods already on
   the list are left out of the suggestions, and typing a name the catalog does
   not know still adds it as a custom item on Enter.
** Fixes
1. The search field in the Longevity Foods, Kitchen, and Recipes columns now
   sits in a fixed band directly under the column's title bar rather than at the
   top of the scrolling area. It rides closer to the title, and the foods,
   recipes, or panes below scroll cleanly beneath the band instead of showing
   through it — the band does not scroll, so nothing passes under the field.
2. In select mode, hovering an item badge's selection circle now shows the
   pointer cursor, so it reads as a click target for toggling the selection
   rather than carrying the badge's drag cursor.
* v0.16.0
** Added
1. Type foods straight onto the Shopping List: a standing field under the
   reserved card's heading adds a badge on Enter, so the list fills without a
   trip to the pyramid. A typed name resolves against the catalog the way pasted
   ingredients do — "kale" joins the "Kale" the kitchen tracks, keeping its tier
   colour and stock identity — while anything off-catalog is added as a custom
   item, so the list can hold things the pyramid does not teach. A name the list
   already holds is skipped, and the field clears after each entry, ready for
   the next.
2. The bundled default catalog gains chili paste, salsa roja, vegetable stock,
   and garlic paste among the condiments, alfalfa sprouts among the leafy
   greens, and Malabar spinach joins the comma convention as "Spinach, Malabar".
   Existing kitchens keep their foods; the changes reach only kitchens seeded
   afterward.
* v0.15.0
** Added
1. The bundled default catalog splits more generics into their grocery forms
   under the comma convention — rice into brown, black, red, and wild; lentils
   into red, green, brown, and black; mushrooms into cremini, white, shiitake,
   oyster, enoki, trumpet, and portobello; tomatoes into cherry, Roma, and
   heirloom; olives into green, black, and Kalamata; mustard into Dijon and
   brown; flaxseed into whole and ground; eggplant into Chinese and Indian — and
   gains chili powder, coriander, onion powder, ground turkey, sprouted-grain
   bread, bean sprouts, slivered almonds, peas, more canned beans, and fresh
   pepper varieties (Anaheim, poblano, habanero). Existing kitchens keep their
   foods; the changes reach only kitchens seeded afterward.
** Fixes
1. The Auto staples and the bundled recipes' ingredient chips follow the
   catalog's renames, so their chips keep their tier colour, stock tracking, and
   shopping-list identity, and "Lemons" — dropped by accident amid the renames
   while seven recipes still call for it — is restored to the catalog.
2. Every bundled recipe ingredient that had quietly stopped matching the catalog
   after the earlier whole-foods refinement is re-pointed at its current entry —
   fifty-two chips across a dozen names, from "Greek yogurt" to "Yogurt, Greek"
   and generic bell peppers, chicken, and turkey to the colour, cut, or form
   each recipe's own text calls for — so every ingredient again carries its tier
   colour, stock tracking, and shopping-list identity.
** Maintenance
1. Seed edits that break names are now caught at both ends: `just seed-save`
   audits the session right after saving and lists every recipe ingredient and
   Auto staple the edits orphaned, `just seed-check` runs the same audit on
   demand, and new server seed-integrity tests enforce the contract in `just
   test` and CI — a catalog rename can no longer silently strip chips of their
   tier colour, stock tracking, and shopping-list identity. The first run of the
   new tests also caught the Mediterranean parsley staple pointing at the fresh
   leafy-green form; it now names the dried, spice-rack form the staples design
   calls for.
* v0.14.0
** Maintenance
1. Bump schemars from 0.9.0 to 1.2.1
   (https://github.com/eric-r-inman/food4u/pull/4)
* v0.13.0
** Maintenance
1. Bump rust-template-foundation from `86bbbcb` to `8c7b32b`
   (https://github.com/eric-r-inman/food4u/pull/19)
* v0.12.0
** Fixes
1. Each column's search bar now stays pinned under its title bar instead of
   scrolling away with the foods, recipes, or panes below it — so the search
   stays in reach no matter how far down the column you have scrolled.
* v0.11.0
** Breaking
1. The MIND Diet page is removed and its toolbar link with it. The MIND diet
   still runs through the app the way the other patterns do — a recipe tag to
   filter by and the Auto staples list that leads the tracker's menu — so the
   standalone page was redundant.
* v0.10.0
** Added
1. A MIND Diet page, linked from the toolbar beside About: a short account of
   the diet and, in the pyramid's own tier colours, its full food lists — the
   ten Eat groups with their weekly targets, the five scored Limit ceilings, and
   an Avoid column for what the studies effectively scored at zero. The three
   columns sit side by side on a wide screen and stack one above another on a
   phone.
2. Four MIND-compliant recipes join the bundled collection, each built on the
   diet's signature foods and unlike anything already there: Walnut-Crusted
   Chicken Breast, Mujadara (lentils and rice under crispy onions), a
   Strawberry-Spinach Salad with Walnuts, and Blueberry-Walnut Oat Muffins. All
   four carry the MIND tag, so the recipe filter and the diet speak the same
   vocabulary.
3. The MIND diet — the brain-focused pattern — now leads the Staples Tracker's
   Auto menu and stocks a fuller list. Following the MIND trial's food groups,
   it gains berry, nut, whole-grain, and bean variety (raspberries and
   blackberries; pecans, cashews, and pistachios; barley and bulgur; canned
   kidney and cannellini beans and edamame), canned omega-3 fish (sardines,
   mackerel, tuna) beside the salmon, and turkey alongside chicken. Its fresh
   leafy greens stay off the list on purpose — those are weekly shopping, not
   pantry stock.
** Fixes
1. Several Auto staples had stopped matching the catalog after its recent
   refinement renamed foods, so those chips no longer picked up their tier
   colour, stock tracking, or shopping-list identity. Every diet's staples are
   re-synced to the catalog's current spellings — "Yogurt, Greek", "Paprika,
   smoked", "Sesame oil", and "Chicken, breast".
* v0.9.0
** Maintenance
1. Bump clap from 4.6.3 to 4.6.4
   (https://github.com/eric-r-inman/food4u/pull/18)
* v0.8.0
** Added
1. The bundled default catalog is refined toward specific, shoppable whole
   foods. Generic entries split into their grocery forms under the comma
   convention — bell peppers into green, red, and yellow; chicken and turkey
   into their cuts; potatoes into Russet, Yukon, and red; onions into red,
   sweet, white, yellow, and green; parsley into Italian, curly, and dried — and
   single forms are renamed to match ("Cheese, goat", "Yogurt, Greek", "Paprika,
   smoked", "Sesame oil", "Rapeseed oil", "Artichoke"). New whole foods join —
   coffee, lobster, roe, salami, broccoli and mustard sprouts, carrot, pea, and
   radish greens, canola oil, wheatgrass, and skyr — while prepared snacks that
   were never whole foods drop out (guacamole, hummus cups, dolmas,
   dark-chocolate almonds). Existing kitchens keep their foods; the changes
   reach only kitchens seeded afterward.
* v0.7.0
** Added
1. A "Pare" toggle beside "Count" gates every delete control. With it off — the
   default — no item badge or category shows a ✕, so badges stay clean and a
   stray click can never delete a food or category. Turning it on reveals the ✕
   on each item badge and category header for deliberate pruning. The ✕ now sits
   in the badge's flow rather than overlaying its right edge, so it never covers
   the name or count.
* v0.6.0
** Maintenance
1. Bump rust-template-foundation from `65f9706` to `86bbbcb`
   (https://github.com/eric-r-inman/food4u/pull/17)
* v0.5.0
** Added
1. The bundled default catalog renames two Longevity Foods categories: the
   Foundation tier's "Oils & healthy fats" becomes "Plant-based Oils & Fats",
   and the Occasionally tier's "Limit" becomes "Snacks". Existing kitchens keep
   their own category names; the rename reaches only kitchens seeded afterward.
** Fixes
1. Toggling a recipe's bookmark no longer changes the card's height. The set
   state used to enlarge the bookmark glyph, which grew the header row and
   nudged the whole card taller; the glyph now keeps a constant size and the set
   state stands out through its filled pill alone.
2. A search in the Longevity Foods column now collapses every category that has
   no match, even ones you had opened, and expands only the categories whose
   foods match — so the results stand alone instead of mixed in with unrelated
   open categories. Clearing the search restores your categories as they were.
* v0.4.0
** Maintenance
1. Bump clap from 4.6.0 to 4.6.3
   (https://github.com/eric-r-inman/food4u/pull/12)
* v0.3.0
** Added
1. Give an item a count: a "Count" toggle beside "Select" turns on an up/down
   stepper at the left of every badge in the Kitchen, Recipes, and Shopping List
   columns, and the badge shows the tally beside its name — "Jicama (3)". The
   Longevity Foods pyramid is a catalog, not a tally, so its foods carry no
   count. Counts start at one, never go below it, and are saved with your
   kitchen.
* v0.2.0
** Maintenance
1. Bump rust-template-foundation from `3aa551c` to `65f9706`
   (https://github.com/eric-r-inman/food4u/pull/11)
* v0.1.0
** Added
1. Sync with the project template (rust-template 65f9706): the standard Publish
   and Release-binaries GitHub workflows are added — inert here, since the
   project ships no release binaries — and the contribution docs gain the
   template's Release-binaries and changelog-gate conventions. The Windows
   cross-build the template now ships is opted out (the server embeds an SQL
   seed the cross helper drops, and a hosted Linux service ships no Windows
   binary), recorded honestly under compliance-ignores. No adopter action.
2. A food's alternate grocery forms are named base-first now — "Salmon, canned",
   "Figs, dried", "Tomatoes, canned, diced" — so an alphabetized Longevity Foods
   list keeps every form beside the food it comes from rather than scattering
   them under "Canned", "Dried", and the like. More canned staples join along
   the way: red, great northern, and white beans, crushed tomatoes, and jarred
   tomato sauce.
3. The Longevity Foods catalog gains the pantry forms of foods it already
   teaches, where those forms are common in longevity cooking: canned tomatoes,
   beans, and mackerel; jarred roasted red peppers and artichoke hearts; whole-
   grain and nut flours (rye, whole-wheat, buckwheat, oat, chickpea, almond,
   cornmeal); dried figs, apricots, cranberries, and shiitake; almond and oat
   milk; applesauce; smoked salmon and mackerel; brown rice flour; and ground
   ginger and garlic powder. Each is its own entry with its own stock tracking,
   beside its fresh counterpart.
4. Stock the Staples Tracker with one press: an "Auto" button under its title
   bar offers the five longevity diets the recipes are tagged with
   (Mediterranean, Blue Zone, MIND, DASH, Anti-Inflammatory), and picking one
   fills the tracker with that diet's essential staples. Staples already present
   are never duplicated, so re-running a diet is harmless and stacking two diets
   yields their union.
5. A Donate page, linked from the toolbar beside About: a placeholder — "Work in
   progress" — while a way to support the app is set up.
6. Clear or export the whole week from the Meal Planner: a toolbar under its
   title bar, matching the Shopping List's, adds an "Export to .txt" button and
   a "Clear all" button that empties every day's slots in one press. The
   exported file lays out the plan day by day, then prints the full text of
   every recipe it draws on, so it is enough to cook the week from on its own.
7. Pasted recipes now read far more of the wild's formats — checkbox and dash
   bullets, "(15-ounce)" can sizes, fused units like "28-ounce", price notes,
   "or"-alternatives, subsection labels, and "juice of 1 lemon" — and every
   parsed ingredient resolves against the Longevity Foods catalog, so "tomato"
   joins the "Tomatoes" your kitchen already tracks instead of arriving as a
   lookalike. Near-misses never merge: coconut milk stays distinct from coconut,
   olive oil from olives.
8. On a narrow screen, where the columns stack instead of sitting side by side,
   a collapsed column now lies flat as a full-width bar with its title reading
   horizontally; the sideways title is only for the side-by-side layout.
9. The server ships as a single self-contained binary: the compiled frontend is
   embedded into it at build time, so a release needs no asset directory
   alongside — and in development the assets are still read live from disk.
10. Arrange the columns however you like: drag a column by its title bar (or its
    collapsed bar) and drop it on another column to take that side of it — a
    vertical line previews where it will land. The arrangement is saved with
    your kitchen, so it follows your account.
11. An About page describes the app, its philosophy, and each column, linked
    from the toolbar next to the title.
12. Breakfast, Lunch, and Dinner are meal tags now, not recipe categories: the
    recipe box organizes by dish type alone, and the tag filter slices by meal
    instead. Recipes previously filed under a meal re-home themselves to a dish
    category with their meal as a tag. The bundled defaults also gain six
    pressure-cooker and slow-cooker dishes, tagged "Instant Pot" and "Slow
    Cooker".
13. An expanded recipe card shows its full name on a line of its own beneath the
    header row, and recipe names are capped at 48 characters so that line always
    fits.
14. Plan the days ahead in a new Meal Planner column between Recipes and
    Kitchen: a pane per day ("Day 1" onward — add or remove days, up to ten),
    each with Breakfast, Lunch, Dinner, and Snacks slots. Drag a recipe card
    into a slot to plan it — the recipe is copied by reference, so it stays in
    the Recipes column and the same recipe can be planned any number of times.
    A planned entry is a full-width card: its magnifying glass jumps to the
    recipe in the Recipes column and scrolls it into view, and its ✕ unplans it.
    Days collapse like panes, and the whole column collapses like its
    neighbours.
15. The bundled defaults now open onto a stocked recipe collection: six recipes
    in every category, drawn from Mediterranean, Blue Zone, MIND,
    anti-inflammatory, and DASH cooking, each tagged by its diet plan, by
    convenience (quick, one-pot, no-cook), and by any featured fish, poultry, or
    eggs — along with the few pantry foods they needed added to the Longevity
    Foods pyramid.
16. Edit the bundled default foods and recipes visually: ~just seed-edit~ opens
    the app on a throwaway copy of the defaults, and ~just seed-save~ bakes what
    you changed back into the shipped seed.
17. Export a recipe to a plain-text file from the ⬇ button in its header; the
    file is named after the recipe and lists its tags, ingredients, and
    instructions.
18. Tag recipes with your own labels and filter by one: tags are edited as chips
    in the expanded recipe card, and a dropdown beside "Search recipes" narrows
    the list to recipes carrying the chosen tag, re-counting each category to
    match.
19. Reorder recipes by dragging: drop a card onto another to slot it in just
    above, or onto a category to send it to that category's end — across
    categories too. The whole card floats with the pointer as you drag it by its
    grip — the same feel as dragging a food badge — a blue line marks where it
    will land, and the card's own category stays open while it drags.
20. Each Longevity Foods tier header shows how many of its foods are in stock
    beside its food count.
21. Bookmark a recipe from its header, between the drag grip and the cart button
    — a marker for remembering a recipe, such as one whose ingredients you have
    just sent to the Shopping List. A recipe category's title row shows how many
    of its recipes are bookmarked. Bookmarked recipes read at a glance as a
    light-blue pill.
22. Select several items and move them together: a "Select: off / on" toggle in
    its own row under the page title turns on a tap-to-select circle on every
    item badge. Move the whole selection either by dragging one selected badge,
    or — with no dragging at all, so it works on a phone — by pressing the green
    ⬇ "Move selected here" arrow that appears on each Kitchen pane, Longevity
    category, and Shopping List category while items are selected; the items
    stay selected so you can send them somewhere else too. While on, the toggle
    counts the current selection — "Select: on (5)". A "Deselect all" button
    appears beside the toggle once more than one item is picked. Badges still
    drag individually with a click-and-hold. The Select row stays pinned on
    screen with the title bar wherever the page itself scrolls.
23. Organize the Shopping List into your own categories: it starts with a
    default set (Produce, Canned & Dry Goods, Dairy, and so on), and you can add
    your own, drag to-buy items into any of them, and delete a category — with a
    confirmation — from the ✕ on its title row.  Each category collapses to a
    labelled row with a count, matching the Recipes column.  Recipe and staple
    additions still land in the main list, ready to sort.  (The automatic
    grocery-department grouping is replaced by these categories.)
24. Delete a Longevity Foods category with the ✕ on its title row, with a
    confirmation.
25. Add your own food category to any tier of the Longevity Foods pyramid, then
    fill it with foods like the built-in categories.
26. Generate recipes with AI: an "AI" button in each recipe category opens an
    assistant that creates a recipe from the meal category, your remembered
    preferences and allergies, and — if you like — what is in your Kitchen; you
    preview it before it is added, and its missing ingredients can go straight
    to the Shopping List.  You bring your own LLM key (Google Gemini's free
    tier, OpenAI/ChatGPT, or Anthropic Claude); the request goes from your
    browser directly to the provider and your key is kept only in your browser.
27. Sign in and sign out through an OIDC identity provider, with each account
    keeping its own kitchen.
28. Add and remove Kitchen storage panes.
29. Edit a Kitchen pane's name and description inline — a pencil opens the
    editor, Enter saves, and Escape cancels without saving.
30. A Staples Tracker pane in the Kitchen: drag in the foods you always want on
    hand, see the ones you are out of turn red, and send those to the Shopping
    List with one button.
31. Move a recipe to another category by dragging it there; categories highlight
    as you drag over them.
32. Seed a new account with the default storage panes on its first sign-in.
33. =just login-demo= runs the app against a local identity provider for trying
    the signed-in experience.
34. Rename the app to Longevity Pantry (the code keeps the =food4u= name).
35. Recolour a Kitchen pane's header from the app's palette while editing it,
    and delete a pane — with a confirmation — from the same editor.
** Fixes
1. The packaged build no longer cross-compiles a Windows binary of the server —
   a hosted Linux service ships none, and that build could not carry the SQL
   seed the server embeds — so the release build and its CI smoke check pass
   again.
2. A recipe's cart button is now green only when the recipe has an ingredient
   that is neither in the Kitchen nor already on the Shopping List; when there
   is nothing left to add it is greyed out, so a glance says which recipes still
   need shopping.
3. The "Paste" and "AI" buttons in a recipe category now work: pressing one no
   longer dismisses the add row before the click takes effect.
4. Adding items after a fresh page load — by dragging, by the Staples Tracker
   cart button, and so on — no longer occasionally fails to save; newly created
   items are given ids that cannot collide with existing ones.
5. The Staples Tracker now starts collapsed, and its shopping-cart button stands
   out clearly against the header — and turns green when it holds staples not
   yet in the Kitchen or on the Shopping List.
6. Editing a Kitchen pane no longer clips the controls on the right edge, and
   clicking away from the pane closes the editor without saving.
** Breaking
1. The frontend_path setting (config file, CLI flag, and environment variable)
   is gone: the server no longer serves the frontend from a directory, since the
   compiled assets are embedded into the binary.
